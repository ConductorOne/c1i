# dev/

Scratch space: temp output, test data, debug dumps.

Everything in here is gitignored except this file, so nothing you drop in can be
committed by accident. Scratch at the repository root has no such protection and
fails the build instead — see `TestNoUnexpectedFilesAtRepoRoot`.
